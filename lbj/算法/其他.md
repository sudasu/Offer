# 其他

## 对称交换

```go
/*
1.go的交换代码，相比较其他语言可以不用设置中间变量
2.交换时考虑奇偶情况，以及确定是数组长度/2
*/
func reverse(nums []int){
    l := len(nums)-1
    for i:=0;i<len(nums)/2;i++{
        nums[i],nums[l-i] = nums[l-i],nums[i]
    }
}
```